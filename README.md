# Project_food-app start.

## `npm install`
In both FRONTEND & BACKEND folder to install all dependencies.

## Connect to mongoose database using mogosh 
              &&
## read the FRONTEND's  (README)

